import styled from 'styled-components'

const AppWrapper = styled.div`

  display: flex;
  flex-flow: column;
  height: 100vh;
  align-items: center;
  
  .sticky-footer {
    display: flex;
    flex-flow: column;
    flex: 1;
    align-items: center;
  }

`

export default AppWrapper
